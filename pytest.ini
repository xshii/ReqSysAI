[pytest]
# 默认只跑 tests/test_*.py，排除 smoke_* 等外部依赖测试
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 自定义标记
markers =
    smoke: 烟测试，依赖外部服务（Ollama/API），不纳入回归
    slow: 慢速测试（>5s）

# 默认排除 smoke 标记的用例
addopts = -m "not smoke" --tb=short -q
